
<% @title = "Instructor Login" %>
<%= form_with url: instructors_email_url, local: true, method: :post do %>
    <div>
        <%= label_tag "email" %>
        <%= email_field_tag "email", nil %>
        <button type="submit">Next</button>
    </div>
<% end %>